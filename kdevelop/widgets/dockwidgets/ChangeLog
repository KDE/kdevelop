01 Avg 1999:
  Create.

05 Avg 1999:
  in STabBar    : fixed paint methods;
  in DockManager: fixed activate method;
                  bug in show dockWidget in the STabCtl ( TabDockGroup )
14 Avg 1999:
  new STabCtl widget
  in DockManager: update for new STabCtl; fixed hide/show methods for readDockConfig

15 Avg 1999:
  change in STabCtl

20 Avg 1999:
  add menu support for hide/show dock and read/write configurations data
  method DockMainWindow::dockMenu()

25 Avg 1999:
  add method DockMainWindow::makeDockVisible() and DockMainWindow::makeWidgetDockVisible();
  small change in DockManager for it.
  this method show DockWidget if it hide or switch to it if widget in the STabCtl 

29 Avg 1999:
  Same methos in DockManager make public;
  DockManager may use separate from DockMainWindow;
  add ethod DockMainWindow::insertMainWindowDockToolBar
  this toolbar allow show/hide dockwidget around MainWindowDockWidget
  same  bug fixed...

07 Sep 1999:
  add *.studio workspace file
  little changes  
  
16 Sep 1999:
  change DockButton to DockBottom
  MainWindowDockWidget allow docking/undocking in/from STabCtl
  fixed bug where KNewPanner is hide
