AC_INIT(configure.in)

AM_CONFIG_HEADER(config.h)

AM_INIT_AUTOMAKE(%{APPNAMELC}, %{VERSION})

AM_MAINTAINER_MODE

AM_ACLOCAL_INCLUDE(macros)

GNOME_INIT

AC_PROG_CC
AC_ISC_POSIX
AC_HEADER_STDC
AC_ARG_PROGRAM
AM_PROG_LIBTOOL

GNOME_COMPILE_WARNINGS

AC_SUBST(CFLAGS)
AC_SUBST(CPPFLAGS)
AC_SUBST(LDFLAGS)

AC_OUTPUT(Makefile macros/Makefile pixmaps/Makefile src/Makefile)


