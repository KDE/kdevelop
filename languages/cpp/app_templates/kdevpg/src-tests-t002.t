// Create some test cases in this directory so that you can check if the parser
// is still working after you changed something. For example, this test case
// for Java (public domain, taken from the ANTLR parser generator) is
// quite tricky, an adapted one may reveal weak points in your grammar.
//
// You should try to find or create similar files for the language
// processed by your parser.

/*
class T {
	static { int i; }

	public T() {
		this( (int) (r * 255), (int) (g * 255));
	}

	void foo() {
		final class U { int i; }
		Class c = int[].class;
		Class d = Object[].class;
		t.new T();
		((T)t).method();
		return "[i=" + (value) + "]";
		int q = (int)+3;
		int z = (int)4;
		int y = (z)+5;
		String s = (String) "ff";
		String t = (s)+"blort";
	}
}
*/
