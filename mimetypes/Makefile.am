application_DATA = chm.desktop
applicationdir = $(kde_mimedir)/application

text_DATA = x-fortran.desktop  x-pascal.desktop
textdir = $(kde_mimedir)/text

EXTRA_DIST = $(application_DATA) $(text_DATA)
